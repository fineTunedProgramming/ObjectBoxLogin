<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="HeadlineText" parent="AppTheme">
        <item name="android:textSize">@dimen/_22</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:fontFamily">@font/roboto_regular</item>
    </style>

    <style name="HeadlineText.Primary">
        <item name="android:textColor">@color/colorPrimary</item>
    </style>

    <style name="FormEdittext" parent="AppTheme">
        <item name="android:padding">5dp</item>
        <item name="android:paddingStart">15dp</item>
        <item name="android:paddingEnd">5dp</item>
        <item name="android:textSize">@dimen/_16</item>
        <item name="android:fontFamily">@font/roboto_light</item>
        <item name="android:background">@drawable/channel_edittxt_bg</item>
    </style>

    <style name="Button" parent="Base.Widget.AppCompat.Button">
        <item name="android:textSize">@dimen/_18</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:fontFamily">@font/roboto_light</item>
    </style>

    <style name="Button.SignIn">
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/sign_in_btn_bg</item>
    </style>

    <style name="Button.SignUp">
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:background">@drawable/sign_up_btn_bg</item>
    </style>


</resources>
